import javax.swing.JFrame;

public class BinaryTreeWindow extends JFrame{
    public BinaryTreeWindow(String framename){
        super(framename);
        setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        setSize(1800, 800);
        add(new BinaryTreePanel());
        setVisible(true);
    }
}
